interface:
  display_name: "UI Test Runner"
  short_description: "Run scripted UI tests and capture output"
  default_prompt: "Use $test-ui to run the project UI test plan and report the console session."
